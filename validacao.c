#include <stdio.h>

void valida (char valor){
	char guarda;
	criapilha();
	if (expressao[i]=='('){
		empilhar(valor, p);
		}
	else if (expressao[i]==')'{
		if (estavazia(p){
			return 0;
			testee;
		}
		else{
			guarda = p -> fim -> dado;
			if (guarda!= dado){
				desempilhar(p);
				return 1;
			}
			else{
				return 0;
			}
		}
	}
	return 1;
}